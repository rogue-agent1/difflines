# difflines
Text file diff: unified, side-by-side, HTML, and statistics.
```bash
python difflines.py old.txt new.txt          # Colored unified diff
python difflines.py old.txt new.txt --stats   # Statistics only
python difflines.py old.txt new.txt --html > diff.html
```
## Zero dependencies. Python 3.6+.
